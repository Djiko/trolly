<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/menu_item_checkout"
        android:icon="@drawable/basket_fill"
        android:title="@string/checkout"
        app:showAsAction="always"/>

    <item android:id="@+id/menu_item_clear"
        android:icon="@drawable/basket_unfill"
        android:title="@string/clear_list"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/menu_item_preference"
        android:icon="@drawable/settings"
        android:title="@string/preferences"
        app:showAsAction="ifRoom"/>

</menu>